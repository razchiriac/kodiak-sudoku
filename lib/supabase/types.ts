// Minimal Database type stub. We don't generate types from Supabase
// because all queries go through Drizzle, not the supabase-js client.
// The placeholder shape lets the typed clients compile without error.
export type Database = Record<string, unknown>;
